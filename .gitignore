# Secrets / credentials — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key

# AWS
.aws-sam/
cdk.out/
cdk.context.json

# Node
node_modules/

# Media / recordings — host demos externally, keep the repo lean
recordings/
*.mp4
*.webm
*.mov

# OS
.DS_Store
