import { ComingSoon } from "@/components/app/coming-soon";

export default function MessagesPage() {
  return (
    <ComingSoon
      emoji="✉️"
      title="Messages"
      body="DM friends about finds, coordinate group gifts, and plan surprises — all in one place."
    />
  );
}
