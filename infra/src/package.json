{
  "name": "giftmaxxing-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Giftmaxxing API Lambda. DynamoDB + lib-dynamodb come from the nodejs20.x runtime; @aws-sdk/client-s3vectors and @aws-sdk/client-bedrock-runtime are bundled (not in the runtime SDK).",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@aws-sdk/client-s3vectors": "^3.700.0",
    "@aws-sdk/client-sns": "^3.700.0",
    "jose": "^5.10.0"
  }
}
